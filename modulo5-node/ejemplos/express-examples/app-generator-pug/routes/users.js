var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function app.use('/', indexRouter);
app.use('/users', usersRouter);(req, res) {
  res.render('user', 
  	{ message: 'Una peticion a usuario'});
});

module.exports = router;
